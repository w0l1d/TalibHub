services:
  postgres:
    image: 'postgres:latest'
    container_name: 'postgres-test'
    environment:
      - 'POSTGRES_DB=talibhubDB'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=ilisi2023-2024'
    ports:
      - '5432:5432'
    networks:
      - 'postgres-test-network'
    volumes:
      - 'postgres-test-data:/var/lib/postgresql'

  keycloak:
    image: 'bitnami/keycloak:latest'
    container_name: 'keycloak-test'
    ports:
      - '9090:8080'
    networks:
      - 'postgres-test-network'
    environment:
      - 'KEYCLOAK_ADMIN_USER=admin'
      - 'KEYCLOAK_ADMIN_PASSWORD=admin'
      - 'KEYCLOAK_DATABASE_HOST=postgres-test'
      - 'KEYCLOAK_DATABASE_PORT=5432'
      - 'KEYCLOAK_DATABASE_NAME=talibhubDB'
      - 'KEYCLOAK_DATABASE_USER=ilisi2023-2024'
      - 'KEYCLOAK_DATABASE_PASSWORD=secret'

networks:
  postgres-test-network:
    driver: bridge

volumes:
  postgres-test-data: